let x = 0;
let y = 0;
let z = 0;
fn (x: str, y: str) => num { 
  x = 'imastring'; // fine, x is a str within the function's scope
  y = true; // error because y is a str
  return 0;
};

{
  x = true; // error because x is a num
}
